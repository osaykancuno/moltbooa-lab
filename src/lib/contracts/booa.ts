import { BOOA_CONTRACT } from "@/lib/constants";

// Minimal ERC-721 subset used by the Studio to verify ownership and list
// tokens owned by a wallet. Only read functions are declared — we never
// initiate transfers from this app.
export const booaAbi = [
  {
    type: "function",
    name: "ownerOf",
    stateMutability: "view",
    inputs: [{ name: "tokenId", type: "uint256" }],
    outputs: [{ name: "", type: "address" }],
  },
  {
    type: "function",
    name: "balanceOf",
    stateMutability: "view",
    inputs: [{ name: "owner", type: "address" }],
    outputs: [{ name: "", type: "uint256" }],
  },
  {
    type: "function",
    name: "tokenOfOwnerByIndex",
    stateMutability: "view",
    inputs: [
      { name: "owner", type: "address" },
      { name: "index", type: "uint256" },
    ],
    outputs: [{ name: "", type: "uint256" }],
  },
] as const;

export const booaAddress = BOOA_CONTRACT as `0x${string}`;
